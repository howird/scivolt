# Random Processes
a rule for assigning a real-valued time function to each outcome $s in cal(S)$
ex: we have an experiment to transmit 3-bit message $T_x->R$
the sample space is $cal(S) = {S_0=(0,0,0), ... S_7=(1, 1, 1)}$

- a collection of random variables "bigger" than a random vector
    - random variable $X(s)$
    - random vector $bold(X) = (X_1 (s), X_2 (s), ..., X_K (s))$
    - random vector $X(s, t) = {X_t (s); t in I}$
$$
X: cal(S) -> R_x subset.eq RR^I
$$

random-variables 